// 출력에 필요한 header를 include
#include <iostream>
// std (standard)라는 namespace (공간)을 사용
using namespace std;

// main 함수 - 기본적으로 실행되는 함수
int main()
{
  // Hello World를 출력
  cout << "Hello World";
  // 정상적으로 완료되었다는 의미로 0을 반환(return)
  return 0;
}